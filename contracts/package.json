{
    "name": "orbid-contracts",
    "version": "1.0.0",
    "description": "OrbIdSwapRelay smart contracts",
    "scripts": {
        "compile": "hardhat compile",
        "deploy": "hardhat run scripts/deploy.ts --network worldchain",
        "deploy:local": "hardhat run scripts/deploy.ts --network hardhat",
        "verify": "hardhat verify --network worldchain",
        "test": "hardhat test"
    },
    "devDependencies": {
        "@nomicfoundation/hardhat-toolbox": "^4.0.0",
        "@openzeppelin/contracts": "^5.0.0",
        "dotenv": "^16.3.1",
        "hardhat": "^2.19.0",
        "typescript": "^5.3.0"
    }
}